// Forced redeploy: 2024-07-26 10:00:00 UTC
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Temporarily simplified for debugging.
    // Previous rule: function isOwner(userId) { return isSignedIn() && request.auth.uid == userId; }
    function isOwner(userId) {
      return isSignedIn();
    }

    match /users/{userId} {
      // Temporarily allowing any authenticated user to read/write for debugging.
      allow read, write: if request.auth != null;
    }

    match /users/{userId}/analytics/{analyticsId} {
      // Temporarily allowing any authenticated user for debugging.
      allow get, list, create, update, delete: if request.auth != null;
    }
    
    match /users/{userId}/apiKeys/{apiKeyId} {
      // Temporarily allowing any authenticated user for debugging.
      allow get, list, create, update, delete: if request.auth != null;
    }
    
    match /users/{userId}/senders/{senderId} {
      // Temporarily allowing any authenticated user for debugging.
      allow get, list, create, update, delete: if request.auth != null;
    }
  }
}
